.env にGEMINIのAPIキーを配置したので，これを用いて論文のPDFを読む際に便利なアプリを作ってほしい
・目的：英語の勉強 + 専門分野の勉強として論文を快適に精読したい．
・基本の UI は通常の pdf ビューワ．
・論文を読みやすいよう，拡大や縮小，上下左右へのドラッグはもちろん，画面のヘッダがないようにする．以下で設計するアプリの主要機能や設定のトグルボタンなどの UI は，画面の左端に縦長に配置する．

・論文は基本的にリポジトリ内の papers ディレクトリにユーザが pdf で配置する．
・論文の pdf はファイル名がわかりにくいため，pdf のファイル名と，その論文のタイトルと和訳したタイトル，研究者名と各研究者の所属研究機関名一覧，投稿された雑誌名，全文の要約，ページごとの要約を LLM を用いて生成し，（さらに便利な情報もあればそれらとともに）メタデータとしてファイル名と紐づける．紐づけ方は json でも何でもよい．ファイルの追加を検出するタイミングは，簡単で負荷の低いものでよい．
・これによって，ユーザは UI 上ではファイル名ではなく和訳したタイトル名で論文を選択できるようにする．
・可能なら，メタデータには参考文献の一覧も抽出したいが，膨大になるためファイルを分けてもよい．
・論文全体の要約は，全文を LLM に渡す必要があるのか，あるいはもっと効果的なやり方があるのかは最新の LLM 関係のツールを見て判断してほしい．
・ただし，単に abstract の部分を和訳するのとはだいぶ視点が異なり，以下の各質問にそれぞれ1~2文で答えるようにしてほしい．十分に理解できていない/読み始めていない論文についても，他の論文との関係（引用関係や，節目となった論文/最新寄りの論文の関係）やアプローチの違いが一目でわかるものにしたい．
　(1) その論文が解こうとしている問題
　(2) 既存の方法では解けないところをその論文はどのようなアプローチで解こうとしているのか（なぜベストな貢献になるのか．先行研究の研究者名，年の部分は正確に．）
　(3) 実験/解析方法のポイント（実験参加者の背景，解析対象のデータについて，除外した条件など）
　(4) その解析方法だと何がわかるのか（数学的にはどうなるとうまくいったといえるのか）
　(5) 実験・解析結果とその解釈
　(6) 議論（考察）や結論で筆者が自分の研究の期待通りの側面と足りない側面（条件設定等も含めて）をどのように捉えているか


メイン機能
・通常のビューワはダブルクリックするとクリック位置にあった「単語」が選択されるため技術的には可能だと思うが，今回のアプリではシングルクリックでクリック位置にあった「文」あるいは「段落」を範囲選択する．文や段落のパースは正規表現や最新のビューワの実装で使われているライブラリ等を頼ってよい．
・文か段落かは UI のトグルボタンから切り替え可能とする．
・画面上をクリックすることで，その文字が含まれる文 or 段落を，論文の専門領域の文章として適切な和訳と，かみ砕いた詳細な解説を UI に表示してくれるようにする（縦スクロール可能）．論文がもともと日本語の場合は和訳は生成しない．
・その際，LLM には論文全体の要約と，そのページの要約を渡すことで文脈を教える．入力する要約には適切な文字数があると思うので要約生成時にコントロールしてほしい．
・LLM は一回で呼び出し，複数呼び出しはしないし記憶も保持しない．
・ユーザがどの論文のどこまでを読めたのか，というのもログとして蓄積したい．参考文献や文章中の補足などを除いて全文を読むのが理想だが，たくさんの論文を短時間で読む必要があるので現実的でないかもしれない．どのような読み方だと論文を理解できたといえるのかわからないが，管理しやすく，成長しやすくなるようにしてほしい．

制約
・実装の言語は問わない．できるだけ，アプリとしての共有や実行が簡単（かつ，実行環境の整備が簡単）で，モダンな UI が実装できるものが好ましい．
・実行環境は windows

オプション機能1
・どの論文を読んでいるときに，ユーザがどんなことに興味を持ったのか，というのをログとして蓄積したい．
・前提として，エージェントに対して質問ができる機能を搭載したい．
・適切な MCP tool （可能なら公開されている高性能なもの）を使用して，ユーザが興味を持ったことに応える情報を取ってくるまで，該当の論文や他の論文を検索してくれる機能を作ってほしい．
・自由質問の会話ウィンドウ（入力欄）を出しておくといいかも．上述のメイン機能である段落の和訳+かみ砕いた解説は，LLM の単発呼び出しの機能として，出力はエージェントとの会話スレッドに出力すれば同じコンポーネントを共通利用できるはず．
・こちらは会話としての記憶を小規模でいいので保持し， 「さっきの」などの入力に対しても意図をくみ取れるようにする．
・エージェントへの入力コンテキストが大きくなる場合は，chroma 等を用いてベクトル DB 化してもよい．

追加機能2
・上記で蓄積したログに応じて，自動で引用先，発展先の論文，あるいは逆に，引用元となった重要な論文を取得してくれる機能があったらよりよい．
・精読には時間がかかるが，私で言えば EEG，機械学習，VLM（マルチモーダルモデル），数学的な問題解決に優れた高性能 LLM の領域の優れた論文をできるだけ多く読み尽くしたいし，他のユーザにもそれぞれ読み尽くしたい領域があると思うので，ユーザの興味の方向性に合わせつつ，できるだけ効率よく新しい論文を調達してくる機能があると毎日楽しい．
・pdf のダウンロードが無理でも，Gemini や MCP tool 側が全文にアクセスできるのであれば，上で指定した要約の (1)~(6) だけでも習得できるようにがんばってください．